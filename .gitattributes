# Excel ZIP Unlocker - Git 屬性設定
# 版本: 1.0
# 說明: 設定 Git 檔案屬性和處理方式

# ===========================================
# 文字檔案處理
# ===========================================

# Python 檔案
*.py text eol=lf
*.pyw text eol=lf

# 設定檔案
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.ini text eol=lf
*.conf text eol=lf
*.cfg text eol=lf
*.toml text eol=lf

# 文件檔案
*.md text eol=lf
*.txt text eol=lf
*.rst text eol=lf

# 腳本檔案
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.sh text eol=lf

# ===========================================
# 二進位檔案
# ===========================================

# 執行檔
*.exe binary
*.dll binary
*.so binary
*.dylib binary

# 壓縮檔案
*.zip binary
*.rar binary
*.7z binary
*.tar binary
*.gz binary
*.bz2 binary
*.xz binary

# Office 檔案
*.xlsx binary
*.xls binary
*.xlsm binary
*.xlsb binary
*.doc binary
*.docx binary
*.ppt binary
*.pptx binary

# 圖片檔案
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.bmp binary
*.tiff binary
*.ico binary
*.svg binary

# 音訊和視訊檔案
*.mp3 binary
*.mp4 binary
*.avi binary
*.mkv binary
*.wav binary
*.flac binary

# ===========================================
# 語言特定設定
# ===========================================

# Python
*.py linguist-language=Python
*.pyw linguist-language=Python

# YAML
*.yaml linguist-language=YAML
*.yml linguist-language=YAML

# JSON
*.json linguist-language=JSON

# Markdown
*.md linguist-language=Markdown

# PowerShell
*.ps1 linguist-language=PowerShell

# Batch
*.bat linguist-language=Batchfile

# ===========================================
# 檔案大小和差異處理
# ===========================================

# 大型檔案使用 Git LFS
*.zip filter=lfs diff=lfs merge=lfs -text
*.rar filter=lfs diff=lfs merge=lfs -text
*.7z filter=lfs diff=lfs merge=lfs -text
*.tar filter=lfs diff=lfs merge=lfs -text
*.gz filter=lfs diff=lfs merge=lfs -text
*.bz2 filter=lfs diff=lfs merge=lfs -text
*.xz filter=lfs diff=lfs merge=lfs -text

# Excel 檔案使用 Git LFS
*.xlsx filter=lfs diff=lfs merge=lfs -text
*.xls filter=lfs diff=lfs merge=lfs -text
*.xlsm filter=lfs diff=lfs merge=lfs -text
*.xlsb filter=lfs diff=lfs merge=lfs -text

# 大型圖片檔案
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text

# ===========================================
# 檔案權限
# ===========================================

# 執行權限
*.py text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# ===========================================
# 特殊處理
# ===========================================

# 設定檔案不進行差異比較
config/config.yaml -diff
config/*.yaml -diff
config/*.yml -diff

# 密碼檔案不進行差異比較
*.passwords -diff
*.keys -diff
passwords.txt -diff
password_list.txt -diff

# 日誌檔案不進行差異比較
logs/*.log -diff
*.log -diff

# 報表檔案不進行差異比較
report/*.yaml -diff
report/*.json -diff
report/*.csv -diff

# ===========================================
# 檔案合併策略
# ===========================================

# 設定檔案使用 union 合併策略
config/config.yaml merge=union
config/*.yaml merge=union
config/*.yml merge=union

# 文件檔案使用 union 合併策略
README.md merge=union
*.md merge=union

# ===========================================
# 檔案過濾
# ===========================================

# 敏感檔案不進行差異比較和合併
config/config.yaml -diff -merge
*.passwords -diff -merge
*.keys -diff -merge
.env -diff -merge
.env.* -diff -merge
